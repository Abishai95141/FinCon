"""The eight metrics. Every rate ships with its tier split (CLAUDE.md invariant).

Unimplemented. Filled in by phase P6 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError("P6 — The eight metrics. Every rate ships with its tier split (CLAUDE.md invariant).")
