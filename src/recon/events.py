"""Typed event emission: MatchProven, MatchRejected, ExceptionRaised,
RuleInduced, RulePromoted, AdapterAuthored, IntakeUnverified, CloseBlocked.

Unimplemented. Filled in by phase P8 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError(
        "P8 — Typed event emission: MatchProven, MatchRejected, ExceptionRaised, "
        "RuleInduced, RulePromoted, AdapterAuthored, IntakeUnverified, "
        "CloseBlocked. "
    )
