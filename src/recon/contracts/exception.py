"""Public semver'd contract — changing a field is a breaking change (ADR-002)

Unimplemented. Filled in by phase P1 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError("P1 — Public semver'd contract — changing a field is a breaking change (ADR-002)")
