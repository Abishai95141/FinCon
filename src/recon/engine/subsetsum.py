"""CP-SAT subset-sum. enumerate_all_solutions for E09; bound hit means unproven
uniqueness, never silent.

Unimplemented. Filled in by phase P5 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError(
        "P5 — CP-SAT subset-sum. enumerate_all_solutions for E09; bound hit means "
        "unproven uniqueness, never silent. "
    )
