"""Candidate generation. Must report blocking recall (invariant 6).

Unimplemented. Filled in by phase P4 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError(
        "P4 — Candidate generation. Must report blocking recall (invariant 6)."
    )
