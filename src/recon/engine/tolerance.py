"""Per-match tolerance budget, consumed across tiers, recorded in the proof

Unimplemented. Filled in by phase P3 — see STATUS.md.
Per CLAUDE.md rule 1: this raises rather than returning a plausible value.
"""


def _unbuilt(*_args, **_kwargs):
    raise NotImplementedError("P3 — Per-match tolerance budget, consumed across tiers, recorded in the proof")
